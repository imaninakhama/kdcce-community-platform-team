.DS_Store
*.swp
.idea/
.vscode/

# Uploaded assignment photos — private, never committed (dev DB is
# local/throwaway; the files it references shouldn't live in git either).
backend/instance/uploads/
